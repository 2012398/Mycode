import 'package:flutter/material.dart';

class Feeding_bottle extends StatelessWidget {
  const Feeding_bottle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Nutrition and supl")],
      ),
    );
  }
}

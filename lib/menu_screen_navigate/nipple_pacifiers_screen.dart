import 'package:flutter/material.dart';

class Nipple_Pacifier extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Nutrition and supl")],
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Wet_wipes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Nutrition and supl")],
      ),
    );
  }
}

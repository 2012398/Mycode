import 'package:flutter/material.dart';

class Bath_accesories extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Nutrition and supl")],
      ),
    );
  }
}
